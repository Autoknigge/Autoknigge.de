/*
 * Consent management is handled by Cookiebot CMP.
 * The Cookiebot script is embedded directly in each HTML <head>.
 * This legacy file is intentionally retained for compatibility with older cached references,
 * but it does not load advertising or analytics scripts.
 */
